# Shortform News Aggregator - Environment Variables
# Copy this file to .env and fill in your values

# API Configuration
# Set USE_MOCK_API=true to use mock data (no backend needed)
# Set USE_MOCK_API=false and provide API_URL to use real backend
VITE_USE_MOCK_API=true
VITE_API_URL=http://localhost:3000/api
VITE_API_KEY=your_api_key_here

# Environment
VITE_ENVIRONMENT=development

# ── AI Summarisation — multi-provider fallback chain ──────────────────────────
# Add any combination; the server tries them in order (Gemini → Groq → OpenAI → Cohere)
# and automatically falls back to the next when a provider's free quota is exhausted.

# 1. Google Gemini (free tier: gemini-2.0-flash-lite)
#    https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# 2. Groq (free tier: llama-3.1-8b-instant, ~14 400 req/day)
#    https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# 3. OpenAI (gpt-4o-mini, pay-per-use but very cheap)
#    https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# 4. Cohere (free trial: command-r-08-2024)
#    https://dashboard.cohere.com/api-keys
COHERE_API_KEY=your_cohere_api_key_here

# Supabase (used by frontend for saved articles/keywords, and by backend for search analytics)
#   URL and anon key: Settings → API → Project URL / anon public
#   Service role key: Settings → API → service_role (server-side only — never expose in frontend)
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ── Supabase (user data, keywords, saved articles, search analytics) ──────────
#    https://supabase.com → your project → Settings → API
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Service role key — server-side only (Vercel env vars), never expose to frontend.
# Required to write search analytics from the backend API.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Analytics (Optional)
VITE_GA_TRACKING_ID=UA-XXXXXXXXX-X
VITE_PLAUSIBLE_DOMAIN=shortform.news

# Error Tracking (Optional)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Feature Flags (Optional)
VITE_ENABLE_DARK_MODE=true
VITE_ENABLE_COMMENTS=false
VITE_ENABLE_SOCIAL_SHARE=true

# Cache Configuration (Optional)
VITE_CACHE_DURATION_HOURS=24
VITE_MAX_ARTICLES_PER_REQUEST=30
